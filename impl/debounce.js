/**
 * A minimal clone of https://lodash.com/docs/#debounce
 * To keep things clear, only two arguments are required.
 * @param {function} func Function to be debounced
 * @param {number} wait Timeout
 */

module.exports.debounce = function(func, wait) {
	// *** Implement me! ***
};