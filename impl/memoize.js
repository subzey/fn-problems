/**
 * A minimal clone of https://lodash.com/docs/#memoize
 * To keep things clear, only the first argument is used.
 * The memoized function is expected to accept only one argument
 * and this argument is a string or a number.
 * @param {function} impl Memoized function
 */
module.exports.memoize = function(impl){
	// *** Implement me! ***
};